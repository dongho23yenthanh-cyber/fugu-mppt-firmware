# MQTT broker for fugu_console.py discovery / --mqtt transport.
# Copy to `mqtt.env` (gitignored) and edit. Values already set in the shell take precedence.
MQTT_HOST=192.168.1.200
MQTT_PORT=1882
MQTT_USER=pv
MQTT_PASS=0ffgrid
