topo=buck       # buck, boost
forced_pwm=0    # set to 1 to enable forced PWM mode, converter will run in CCM even at light loads,
pwm_driver=mcpwm # ledc|mcpwm; only consulted when the firmware compiles in both drivers (default ledc)